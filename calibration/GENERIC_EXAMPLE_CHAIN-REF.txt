% Generic example for calibratioon data file (complete chain of DAC and ADC)type = chain % complete chain of DAC and ADC (but no SENSOR included for REF channel)FILE = GENERIC_EXAMPLE_DAC.txt			% DAC calibration dataFILE = GENERIC_EXAMPLE_ADC.txt			% ADC calibration data